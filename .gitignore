# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
# Local Netlify folder
.netlify

# next-env.d.ts is regenerated by Next on every dev/build run, and it flips
# between .next/dev/types and .next/types depending on which one ran last.
# Tracking it produces a diff that means nothing and reverts itself.
next-env.d.ts

# Scratch harnesses used to drive a headless browser during development.
.qa-*.mjs
